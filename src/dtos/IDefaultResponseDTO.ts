export interface IDefaultResponseDTO {
    message: string
  }
  
  export interface IDefaultResponsePaginationDTO {
    currentPage: number
    maxPage: number
    rows: number
    totalRows: number
    message: string
  }